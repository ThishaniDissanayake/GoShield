RATE_LIMIT=100
WINDOW_SECONDS=60
REDIS_ADDR=localhost:6379

# Rate-limit algorithm: "sliding" (default) or "fixed"
RATE_LIMIT_MODE=sliding

# Gateway mode only – set the upstream API URL
UPSTREAM_URL=http://localhost:9000
